# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: 688f5d68-773a-4ad6-9361-73b1c63fab3c

include:
  - resources/*.yml
  - resources/*/*.yml


variables:
  catalog: 
    default: "citibike_dev"
  dev_flag: 
    default: true

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3021718547832009.9.azuredatabricks.net

  test:
    mode: production
    variables:
      catalog:
        default: "citibike_test"
      dev_flag:
        default: false
    presets:
      name_prefix: '[testing] '
    workspace:
      host: https://adb-816856889325369.9.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/yixing.zheng.au@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - service_principal_name: d2e14495-9094-40e8-933e-85d85cca2093
        level: CAN_MANAGE
    run_as:
      service_principal_name: d2e14495-9094-40e8-933e-85d85cca2093

  prod:
    mode: production
    variables:
      catalog:
        default: "citibike_prod"
      dev_flag:
        default: false
    workspace:
      host: https://adb-1547466762834234.14.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/yixing.zheng.au@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - service_principal_name: 1ef5896b-0e1f-4a3b-9500-3dbb375454f8
        level: CAN_MANAGE
    run_as:
      service_principal_name: 1ef5896b-0e1f-4a3b-9500-3dbb375454f8
